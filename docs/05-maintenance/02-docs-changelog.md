# Documentation Changelog

## 2026-07-20

v1.0.0 initial documentation set.

Highlights:

- documentation hub for architecture, getting started, user guide, examples, development, and maintenance
- release process aligned with `jooservices/dto` Git flow (`develop` → `release/<version>` → `master` → tag → back-merge)
- runtime truth for guards/callbacks (`new $class()`), optional PSR-14 events, and readonly property limits
- AI skills map and quality command map synchronized with repository tooling
