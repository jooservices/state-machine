Use `AGENTS.md`, `.github/skills/repo-quality-foundation/SKILL.md`, and `.github/skills/documentation-sync/SKILL.md`.

Topic:

{{input}}

Audit first, then apply only targeted updates.
Update docs and AI guidance from source-of-truth files only.
If facts are unclear, mark them as `Needs clarification`, `Missing decision`, `Assumption`, or `Outdated / should be verified`.
